/**
 * Instruction 9B audit vocabulary — same implementation as {@link VenextAuthzGuard}
 * (org membership, relationship participation, user alignment when headers exist).
 */
export { VenextAuthzGuard as OrganizationAccessGuard } from "./venext-authz.guard";
