/** Instruction 20.55 — deterministic terrain intelligence (re-export). */
export {
  buildActivityHints,
  buildDemandSignals,
  buildPartnerSuggestions,
  buildStockSignals,
  sanitizeGrossisteText,
  type GrossisteHint,
} from "./mocks/grossiste-b-intelligence";
