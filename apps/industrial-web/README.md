# Industrial web (VENEXT)

Instruction 5 ships the **operational pole system** in the active Next.js industrial app:

`apps/web-industrial-nextjs/src/poles/`

This `industrial-web` path is reserved for a future standalone package or app split; today all pole UI, `MapControlEngine`, adaptive workspace shell, and demo data live under **web-industrial-nextjs**.

Path anchor for Instruction 5 §12: `src/poles/CANONICAL.md` in this folder.

Routes: `/poles` (hub) and `/poles/[pole]` (eight pole namespaces).
