import type { PoleIntelSurface } from "../pole-intel-manifest";

const ctx: PoleIntelSurface = {
  summaryLine: "Mémoire stratégique corridor — capitalisation patterns et décisions (20.18).",
  mapHintLine: "Historique explicable et auditable — pas mémoire IA opaque ni scoring social.",
};

export default ctx;
