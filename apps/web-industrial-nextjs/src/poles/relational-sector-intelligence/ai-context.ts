import type { PoleIntelSurface } from "../pole-intel-manifest";

const ctx: PoleIntelSurface = {
  summaryLine:
    "Couche sectorielle corridor-first (20.23) — structure de marché, propagation, pression industrielle — pas ERP ni scoring marketplace.",
  mapHintLine:
    "Lecture infrastructure: concentrations, chaînes de valeur, fragilité opérationnelle — corrélation économique bornée, sans IA générative.",
};

export default ctx;
