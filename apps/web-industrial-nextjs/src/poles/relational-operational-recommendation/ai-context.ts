import type { PoleIntelSurface } from "../pole-intel-manifest";

const ctx: PoleIntelSurface = {
  summaryLine: "Recommandations opérationnelles déterministes — SLA, incidents, gouvernance corridor (20.14).",
  mapHintLine: "Moteur explicable et auditable — pas assistant conversationnel ni notation marketplace.",
};

export default ctx;
