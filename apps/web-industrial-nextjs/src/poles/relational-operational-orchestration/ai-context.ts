import type { PoleIntelSurface } from "../pole-intel-manifest";

const ctx: PoleIntelSurface = {
  summaryLine: "Orchestration opérationnelle déterministe — plans, étapes, validation humaine (20.15).",
  mapHintLine: "Centre de pilotage corridor — séquencement explicable, pas autopilot ni assistant conversationnel.",
};

export default ctx;
