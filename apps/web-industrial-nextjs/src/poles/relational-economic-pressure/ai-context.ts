import type { PoleIntelSurface } from "../pole-intel-manifest";

const ctx: PoleIntelSurface = {
  summaryLine:
    "Cartographie pression & dépendances inter-corridors (20.21) — contagion bornée, audit corridor, pas ERP logistique.",
  mapHintLine:
    "Couche systémique déterministe — saturation coordination, dépendances asymétriques, zones fragilité structurelle.",
};

export default ctx;
