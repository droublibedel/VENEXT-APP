import type { PoleIntelSurface } from "../pole-intel-manifest";

const ctx: PoleIntelSurface = {
  summaryLine: "Revue scénario & comité décisionnel corridor — validation humaine traçable (20.17).",
  mapHintLine: "Approbations documentées — pas autopilot commerce, pas assistant IA conversationnel.",
};

export default ctx;
