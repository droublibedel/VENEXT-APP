"use client";

/** Relationship density strip — graph-field health, not CRM lists (Instruction 5 §4). */
export function RelationshipFieldRibbon() {
  return (
    <div className="mb-1 rounded border border-cyan-500/30 bg-cyan-500/10 px-2 py-1 text-[10px] text-cyan-100/90">
      Network field · retailer density +9% vs void baseline — 2 inactive pockets flagged (mock)
    </div>
  );
}
