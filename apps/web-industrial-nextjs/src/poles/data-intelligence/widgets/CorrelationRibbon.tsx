"use client";

/** Correlation / anomaly teaser — intelligence plane, not BI exports (Instruction 5 §4). */
export function CorrelationRibbon() {
  return (
    <div className="mb-1 rounded border border-emerald-500/30 bg-emerald-500/10 px-2 py-1 text-[10px] text-emerald-100/90">
      Correlation pass · route anomaly ↔ demand spike co-movement p=0.61 (mock, 6h window)
    </div>
  );
}
